# NodeExpress

    // Start the project
    npm init 
    // Install express
    npm i express --save
    // body-parser
    npm install body-parser --save
    // jwt
    npm install jsonwebtoken --save
    npm install express-jwt --save
